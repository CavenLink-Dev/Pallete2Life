// Superseded by static WebP thumbnails in the Examples gallery.
// Kept as a stub to avoid broken imports from any cached references.
import type { InspirationItem } from "../lib/inspirationCatalog"
export default function InspirationThumbnail(_: { item: InspirationItem }) {
  return null
}
